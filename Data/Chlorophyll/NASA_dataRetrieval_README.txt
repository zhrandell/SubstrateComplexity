// Download MODIS data using a bash script 
// download text file (bashscript.sh) from data order; place file is desired location for data download; run the following in Cygwin

ch mod 777 bashscript.sh

./bashscript.sh



// Using cygwin
use http_manifest.txt to download TAR file. 
Place TarFile in desired destination
Use Cygwin to unpack tar files: 

tar -xf file.tar

